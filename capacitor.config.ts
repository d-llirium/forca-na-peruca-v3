import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'forca_na_peruca-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
